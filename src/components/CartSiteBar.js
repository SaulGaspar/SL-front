// Carrito lateral anterior.
// Se deja sin renderizar para evitar duplicar la experiencia del MiniCart actual.
// Usa MiniCart + CartFloatingButton como carrito principal del sitio.

export default function CartSidebar() {
  return null;
}
